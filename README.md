# video-autopilot (redirect only)

The live page is **https://jaygptpro.com/video-autopilot/**, served from the
`jaygptpro.com` repo at `video-autopilot/`. That is the single source of truth;
edit it there.

This repo only holds a redirect so older links keep working.
